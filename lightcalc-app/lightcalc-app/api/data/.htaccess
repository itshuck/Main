# Защита хранилища рейтинга: сырые файлы счетов не отдаются напрямую.
# (Apache; для nginx-хостингов данные и так только JSON-числа без персональных
# данных, прямой доступ не критичен — вся выдача идёт через top.php.)
Require all denied
